package com.myspace.mymortgagedemo;

import java.lang.Number;

rule "Calculate Instalment"
	dialect "mvel"
	when
		app : Application( interest > 0.0 && amortization > 0 && interest > 0.0 )
	then
		app.setInstallment( calculateInstallment (app.amount, app.interest, app.amortization) );
end
